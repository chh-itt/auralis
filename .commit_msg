chore: polish docs, rename tests, bump to 0.1.9

- Rename misleading test names (pending_wakes_* to batch_spawn_*,
  stale_waker_* to executor_drop_recreate_*) to accurately describe
  what is actually tested.

- Document Signal::Debug RefCell panic risk when T Debug impl
  writes back to the same signal.

- Document free_slot invariant: caller must not double-free.

- MSRV job continues to test both crates via --workspace --lib.

- track_caller on timer::sleep deferred (still nightly-gated
  for async fn).
